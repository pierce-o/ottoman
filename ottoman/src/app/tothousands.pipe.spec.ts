import { TothousandsPipe } from './tothousands.pipe';

describe('TothousandsPipe', () => {
  it('create an instance', () => {
    const pipe = new TothousandsPipe();
    expect(pipe).toBeTruthy();
  });
});
