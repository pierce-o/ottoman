import { IondateformatPipe } from './iondateformat.pipe';

describe('IondateformatPipe', () => {
  it('create an instance', () => {
    const pipe = new IondateformatPipe();
    expect(pipe).toBeTruthy();
  });
});
